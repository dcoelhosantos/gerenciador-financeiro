// frontend/next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // (Qualquer outra config que você tinha pode vir aqui)
};

export default nextConfig;
